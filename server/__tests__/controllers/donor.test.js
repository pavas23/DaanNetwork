const path = require("path");
const {
  donorController,
} = require("../../src/api/controllers/DonorController");

it("should send a status code of 400, when user is not found", () => {
  // donorController.createDonationRequest();
  // console.log(donorController.createDonationRequest);
});
